export const exportPdf = async (): Promise<void> => {
  console.info("[exportPdf] PDF export placeholder invoked");

  throw new Error("PDF export is not implemented yet.");
};

export default exportPdf;
